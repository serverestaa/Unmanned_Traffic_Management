import { useSigninMutation } from "@/api/user"

export const SigninPage = () => {

    const [signin] = useSigninMutation();

    // ОСЫ ЖЕРГЕ ФОРМА БРОСКИ

    return (
        <div className="w-full flex flex-col items-center ">

        </div>
    )
}